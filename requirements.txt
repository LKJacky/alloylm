# ============================================================
# alloylm requirements

accelerate>=1.0.0
aiofiles>=23.2.0
aiohttp>=3.9.0
datasets>=2.14.0

# --- Inference Server ---
fastapi>=0.100.0
flashinfer-python>=0.2.0
httpx>=0.25.0
huggingface_hub>=0.20.0
jinja2>=3.1.0

# --- General Utilities ---
loguru>=0.7.0
math-verify>=0.5.0

# --- Training Utilities ---
mmengine>=0.10.0
numpy>=1.24.0
openai>=1.0.0
orjson>=3.9.0
pandas>=2.0.0

# for test
parameterized>=0.9.0
pydantic>=2.0.0

# --- Distributed ---
ray[default]>=2.9.0
requests>=2.31.0
safetensors>=0.4.0
sortedcontainers>=2.4.0
tensorboard>=2.14.0
# --- Core ML & Serving ---
torch>=2.6.0
tqdm>=4.65.0
transformers>=4.51.0,<6
typing_extensions>=4.0.0
uvicorn[standard]>=0.23.0

# ============================================================
# GPU-specific packages (install separately with matching CUDA/torch version)
# ============================================================
#
# flash-attn (requires torch + CUDA, build from source or use pre-built wheels):
#   pip install flash-attn --no-build-isolation
#   or: pip install flash-attn==2.7.4.post1 --no-build-isolation
